---
id: {{FEATURE_ID}}-plan
type: plan
status: draft
feature: {{FEATURE_NAME}}
depends_on: [{{FEATURE_ID}}-spec]
verified_by: [{{FEATURE_ID}}-tasks]
tags: []
created_at: {{DATE}}
updated_at: {{DATE}}
---

# {{FEATURE_NAME}} — Implementation Plan

> **Spec reference:** `{{FEATURE_ID}}-spec` (must have `status: approved` before this plan is created)  
> **Gate status:** Awaiting Gate 2 approval before implementation begins.

## User Review Required

> List anything that requires explicit user decision before implementation starts.
> Use this section to surface architectural trade-offs or irreversible choices.

- [ ] [Decision 1 — e.g., choice of caching strategy]
- [ ] [Decision 2 — e.g., sync vs. async processing]

## Open Questions

> Unresolved questions that could change the file mapping or architecture below.
> Resolve before requesting Gate 2 approval.

- [ ] [Question 1]

---

## Proposed Changes

> Group files by architecture layer. Use `[NEW]`, `[MODIFY]`, or `[DELETE]` markers.
> Order layers from innermost (domain) to outermost (infrastructure/api).

### Layer: Domain / Core

> Pure business logic and entities. No framework dependencies.

#### [NEW] `src/domain/[EntityName].ts`
> Description of what this file contains and why it's needed.

#### [MODIFY] `src/domain/[ExistingEntity].ts`
> Description of what changes and why.

### Layer: Application / Use Cases

> Orchestrates domain objects. Contains use case classes or functions.

#### [NEW] `src/application/[UseCaseName].ts`

### Layer: Infrastructure / Persistence

> Database access, external API clients, file I/O.

#### [NEW] `src/infrastructure/[RepositoryName].ts`

### Layer: API / Interface

> HTTP controllers, GraphQL resolvers, CLI handlers, event listeners.

#### [NEW] `src/api/[ControllerName].ts`

### Layer: Tests

> Test files mirror the source structure under `tests/` or `__tests__/`.

#### [NEW] `tests/domain/[EntityName].test.ts`
#### [NEW] `tests/application/[UseCaseName].test.ts`
#### [NEW] `tests/api/[ControllerName].integration.test.ts`

---

## Data Model & Interfaces

> Define TypeScript interfaces, Java interfaces, Python dataclasses, or JSON schemas here.
> This is the binding contract between layers.

```typescript
// Example interface definition
interface FeatureRequest {
  field: string;
}

interface FeatureResponse {
  id: string;
  field: string;
  createdAt: string;
}
```

---

## Verification Plan

### Automated Tests
```bash
# Replace with the actual test command for this project's stack
npm test
# or: mvn test / pytest / go test ./...
```

### Manual Verification Steps
1. [Step 1 — e.g., deploy to local/staging environment]
2. [Step 2 — e.g., execute the happy path scenario from the spec]
3. [Step 3 — e.g., verify error scenarios return correct HTTP status codes]

