---
name: <stack-name>-stack
description: >-
  Use this skill when developing [technology/framework] projects. It provides
  [technology]-specific patterns, directory conventions, testing setup, and
  code examples optimized for the SDD lifecycle. Activate when working on a
  project that uses [technology] as its primary stack.
# --- SDD Stack Contract ---
sdd_stack_contract:
  test_command: "<replace with exact test command>"
  lint_command: "<replace with exact lint command>"
  setup_command: "<replace with exact setup command>"
---

# Skill: `<stack-name>-stack` — [Technology/Framework] Development

> **Stack Contract Version:** 1.0 (compatible with `sdd-scaffold-plugin` v1.0.0+)

---

## Technology Overview

> Brief description of the technology stack this skill covers.
> Include: language, framework, primary use case, and ecosystem overview.

---

## Project Structure

> Define the canonical directory structure for a new [technology] project.

```text
project-root/
├── src/
│   ├── domain/         # Business entities and interfaces
│   ├── application/    # Use cases and application services
│   ├── infrastructure/ # Database adapters, external service clients
│   └── api/            # HTTP controllers / CLI handlers
├── tests/
│   ├── unit/
│   └── integration/
└── [build-config-files]
```

---

## Stack Contract Implementation

### Test Command
```bash
<replace with exact test command>
```
> Explain any flags used and what the output looks like on success/failure.

### Lint Command
```bash
<replace with exact lint command>
```
> Explain what linting tools are used and what clean output looks like.

### Setup Command
```bash
<replace with exact setup command>
```
> Any prerequisites (language runtime version, global tools, etc.).

---

## TDD Patterns for [Technology]

> Describe how Red-Green-Refactor maps to this stack's testing framework.

### Test Framework Setup

> How to initialize the testing framework in a new [technology] project.

### Writing a Failing Test (RED)

> Example of a minimal failing test in [technology].

```[language]
// Example failing test
```

### Minimum Implementation (GREEN)

> Example of minimum code to pass the test above.

```[language]
// Minimum implementation
```

### Refactoring Patterns

> Common refactoring patterns specific to [technology].

---

## Security Considerations

> [Technology]-specific security concerns that the `security-auditor` subagent should check.

- [ ] [Security concern 1]
- [ ] [Security concern 2]

---

## Examples

See `examples/` directory for complete, working examples of:
- A basic domain entity with unit tests
- A use case with mocked dependencies
- An API endpoint with integration test

